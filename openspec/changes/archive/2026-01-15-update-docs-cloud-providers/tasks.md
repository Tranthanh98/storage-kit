## 1. Update Introduction
- [x] 1.1 Update `docs/guide/introduction.md` to include Amazon S3, Google Cloud Storage, Azure Blob Storage, and DigitalOcean Spaces in the list of supported providers.

## 2. Update Providers API
- [x] 2.1 Update `docs/api/providers.md` to add configuration interfaces for:
    - Amazon S3 (`S3Config`)
    - Google Cloud Storage (`GCSConfig`)
    - DigitalOcean Spaces (`SpacesConfig`)
    - Azure Blob Storage (`AzureConfig`)

## 3. Review Installation
- [x] 3.1 Review `docs/guide/installation.md` (likely no changes needed as dependencies are internal to `@storage-kit/core`, but good to check).
